# TikTok Configuration
# Your TikTok ms_token cookie value (highly recommended for reliable scraping)
# To get this: Log into TikTok in your browser, open DevTools > Application > Cookies
TIKTOK_MS_TOKEN=your_ms_token_here

# Discord Configuration
# Your Discord bot token (required)
# Get this from: https://discord.com/developers/applications
DISCORD_BOT_TOKEN=your_bot_token_here

# The ID of the Discord channel where threads will be created (required)
# To get this: Enable Developer Mode in Discord, right-click the channel, and select "Copy ID"
DISCORD_CHANNEL_ID=your_channel_id_here

# Filtering Configuration
# Minimum follower count for a profile to be posted (default: 100000)
MIN_FOLLOWERS=100000

# Application Configuration
# Number of trending videos to fetch (default: 50)
TRENDING_COUNT=50

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL (default: INFO)
LOG_LEVEL=INFO

# Database file path for tracking posted profiles (default: posted_profiles.db)
DATABASE_PATH=posted_profiles.db
